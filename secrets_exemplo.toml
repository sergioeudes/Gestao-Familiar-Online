# Copie este arquivo para .streamlit/secrets.toml (nova pasta, mesmo nível do appfin.py)
# e troque o valor abaixo pela senha real da família.
#
# No Streamlit Community Cloud, NÃO envie esse arquivo pro GitHub: cole o mesmo conteúdo
# em "Advanced settings" -> "Secrets" na tela de publicação (ou em "Manage app" -> "Secrets"
# depois de publicado). O app lê de lá automaticamente, sem precisar do arquivo no repositório.
app_password = "troque-esta-senha"
